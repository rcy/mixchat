version: "2"
sql:
  - engine: "postgresql"
    queries: "./query.sql"
    schema: "../../data/schema.sql"
    gen:
      go:
        package: "db"
        out: "."
        sql_package: "pgx/v5"
        
